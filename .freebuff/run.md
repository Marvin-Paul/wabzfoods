# Run doc — Wabz Foods (wabz-next)

## How to reproduce uncommitted artifacts

No `.env.local` or secret files are needed. Dependencies are in `node_modules/`.

## How to run the dev server

1. `cd wabz-next`
2. `npx next dev --port 3000`
3. Open http://localhost:3000
